server {
    listen {{ template_webserver_port }};
    server_name localhost;
    charset utf-8;
    client_max_body_size 100M;
    location / {
        include uwsgi_params;
        uwsgi_pass unix:{{ template_uwsgi_socket }};
    }
}